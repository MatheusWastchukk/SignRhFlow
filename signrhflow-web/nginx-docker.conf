server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = /SignRhFlow {
        return 301 /SignRhFlow/;
    }

    location /SignRhFlow/ {
        try_files $uri $uri/ /SignRhFlow/index.html;
    }
}